version: '3.8'

services:
  zbox:
    build: .
    container_name: zbox
    ports:
      - "8000:8000"
    volumes:
      - ./music:/app/music
      - ./data:/app/data
      - ./web:/app/web
    restart: unless-stopped
    environment:
      - TZ=Europe/Warsaw
      - ZBOX_IP=${ZBOX_IP:-}
