#pragma once
#define LOGI(fmt, ...) do {} while(0)
#define LOGW(fmt, ...) do {} while(0)
#define LOGE(fmt, ...) do {} while(0)
#define LOGC(fmt, ...) do {} while(0)
#define LOG(fmt, ...)  do {} while(0)
#define LOGLN(msg)     do {} while(0)
